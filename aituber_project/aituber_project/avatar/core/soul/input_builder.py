#-----------------------------------------------------------------------------------------------------------------#
def build_generate_comment_input() -> str:
    return "VTuber配信の視聴者コメントを1つ作成してください。"
#-----------------------------------------------------------------------------------------------------------------#
#-----------------------------------------------------------------------------------------------------------------#
def build_generate_self_introduction_input() -> str:
    return "設定を考慮しながら、30文字以上、50文字以内で自己紹介をしてください。"
#-----------------------------------------------------------------------------------------------------------------#
#-----------------------------------------------------------------------------------------------------------------#
def build_generate_news_talk_input() -> str:
    return """
以下のルールで生成してください:
- 最初にニュース内容を宣言
- 中程度の長さで説明
- 最後に短い感想
- 120〜220文字
"""
#-----------------------------------------------------------------------------------------------------------------#
#-----------------------------------------------------------------------------------------------------------------#
def build_generate_weather_talk_input() -> str:
    return "設定を考慮しながら、50文字以上、200文字以内で日本のランダムな都市の天気予報を教えてください。文字数が足りない場合、その都市の豆知識も話してください。"
#-----------------------------------------------------------------------------------------------------------------#
#-----------------------------------------------------------------------------------------------------------------#
def build_character_reply_input(username: str, user_message: str, short_term_memory_instruction: str, long_term_memory_instruction: str
) -> str:
    return f"""
【返信相手】
{username}

【最新コメント】
{user_message}

【長期記憶】
{long_term_memory_instruction}

【短期記憶・直近履歴】
{short_term_memory_instruction}

【返信方針】
- 最新コメントへの返答を最優先してください。
- 長期記憶は、ユーザー本人に関する継続的な情報として扱ってください。
- 長期記憶が最新コメントに関係する場合だけ、自然に参考にしてください。
- 短期記憶は、直近の会話の流れを理解するために使ってください。
- 履歴や記憶を無理に要約しないでください。
- ユーザーが話題を変えた場合は、新しい話題に合わせてください。
"""
#-----------------------------------------------------------------------------------------------------------------#
#-----------------------------------------------------------------------------------------------------------------#

def build_LongTermMemory_input(comment: str) -> str:
    return f"""
【ユーザー発言】
{comment}
"""

#-----------------------------------------------------------------------------------------------------------------#
#-----------------------------------------------------------------------------------------------------------------#
def build_script_with_instruction_input() -> str:
    return "設定を考慮しながら、最大300文字で文章を作成してください。"
